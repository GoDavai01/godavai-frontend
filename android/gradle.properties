# Project-wide Gradle settings.

# JVM memory for Gradle (bump a bit to avoid OOM on bundleRelease)
org.gradle.jvmargs=-Xmx3072m -Dfile.encoding=UTF-8

# AndroidX (keep)
android.useAndroidX=true

# ---- GoDavaii release signing configs (presently in repo; see note below) ----
MYAPP_UPLOAD_STORE_FILE=godavaii-upload.keystore
MYAPP_UPLOAD_KEY_ALIAS=godavaii
MYAPP_UPLOAD_STORE_PASSWORD=Pururva15
MYAPP_UPLOAD_KEY_PASSWORD=Pururva15

# ---- Force Gradle to use JDK 17 (ABSOLUTE PATH; double backslashes on Windows) ----
org.gradle.java.home=C:\\Program Files\\Eclipse Adoptium\\jdk-17.0.16.8-hotspot
